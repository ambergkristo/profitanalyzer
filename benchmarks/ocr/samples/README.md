# Sample Setup

Committed OCR sample binaries are intentionally omitted.

For live provider validation, add a synthetic local file to:

- `benchmarks/ocr/private-samples/clean-invoice-provider-sample.jpg`

Requirements:

- use only synthetic/generated invoice imagery
- no real company secrets
- no personal data
- keep the sample aligned with `../expected/clean-invoice.expected.json`
